"""Test suite.

A package rather than a bare directory so fixture modules can be imported explicitly
(``from tests.db_fixtures import ...``) instead of relying on pytest's ``sys.path``
insertion, which type checkers and editors cannot follow.
"""
