# Gap analysis — what is missing, as of 2026-08-20

*Companion: `docs/report-quality.md` tracks whether the output reads as research. This file asks whether the platform is **correct**; that one asks whether its report is **readable by somebody making a decision**. The CHRW run of 2026-08-21 is the first where the two come apart — every correctness gate that mattered passed, and the document still explained itself to its reader.*

*A48–A60 come from the AAPL run of 2026-08-20, the first on the polished code and the
first against a company with real depth. They are a different kind of finding from
everything above them: the guards all held — 122 of 122 citations verified, every refusal
correct, nothing unsourced reaching the page — and what the run exposes is what those
guards **cost** when they misfire on their own housekeeping. Thirteen of sixteen sections
were drafted twice.*

*Updated after A1–A4, then A21, B13 and A9, then B1, A19 and A20, then B2 and B3, then the
test-suite health items A16–A18 and the data-lifecycle items A10–A12, and most recently
A22–A25, which came out of deliberately breaking the invariants to see what noticed, and
A45–A47 and B14 from the AMZN run of 2026-08-18, all four now closed. **The AAPL run's
own findings are now closed too — A50 to A54 and A56 to A60 — leaving three that cannot be
finished at a desk: A48 and A63 have their instrumentation in place and need the next live
run to read it back (A49 was closed by the MTB run doing exactly that), and A55's
concept-map curation is judgement over accounting semantics rather than a code change.** Items struck through are done; the reasoning is kept because a
gap and the shape of its fix are worth reading together.*

*The **first complete** run of the same workflow, later on 2026-08-18, found more than this
register is shaped to hold: a cross-company contamination of the evidence pack, a fiscal
year taken from the wrong field, and a valuation section describing a method nothing
executed. Those and the rest are tracked as a task sequence in `docs/polish-phase-1.md`
rather than as entries here, because they have a dependency order and several share one
root cause.*

*A22 and A23 are worth reading before the rest of List A, because neither was found by a
failing test: both are things the code **said** it did. A guard populated with a ceiling it
never compared, and a docstring promising three call paths composed the same prompt when two
of them did not. A test suite cannot fail on a claim nobody encoded.*

*A26 and A27 came from turning the same method on `src/aer/calc/`, and carry their own
lesson about the method. The first pass reported five escapes; two of them were not holes at
all — the tests existed in a file the selector did not name. **A mutation that survives a
narrow selector has found the edge of the selector, not a gap in the suite**, and the
survivors were only called holes after being re-run against every test file that can
transitively reach the module.*

*Since A16 was closed the whole suite runs in one process again — 4866 unit tests in about
27 minutes and 75 browser tests in about 3, both green (re-measured 2026-08-20; the count
has grown with every batch since) — so the figures quoted below are measured rather than
remembered. The "green in three different file orderings" that stood here has been left
behind by the suite it described: see A28, where a leak found on 2026-08-20 says what that
claim is worth once a test drives a real run. The unit suite is
`pytest --ignore=tests/e2e`, which is what `just test` runs, and the split is not
cosmetic: Playwright's synchronous API drives an asyncio loop on the main thread and holds
it open for the life of its session fixture, so every asyncio fixture afterwards in the same
process dies with "Runner.run() cannot be called from a running event loop". The
`test-all` recipe says so in a comment. **A bare `pytest` is not the gate** — it collects
both halves and reports thousands of errors that say nothing about the code.*

*A30–A33 are the first findings that came from live runs rather than from the suite. A30 and
A31 share a cause: the fake provider is an alternative implementation of the protocol, not a
fake transport, so nothing that only runs against it can see the wire, and nothing that never
exhausts a ceiling can notice one is too low. A33 was found by asking what A31's fix made
worse, which is a question worth asking of every change that raises a limit. A34 is the
same disease's third appearance — a number guessed before measurement — and its fix, by
operator direction, removes the class of number rather than resizing the instance.*

Written after the first end-to-end live runs, from the code rather than from the plan. The
phase specifications in `docs/PLAN.md` remain the authority on *scope*; this document is an
honest account of the distance between that scope and what a run actually does today.

Two lists, as asked: what is missing behind the surface, and what is missing in front of it.

---

## The finding that dominated both lists — now closed

**A large, tested deterministic layer existed that the live workflow never called.**

`src/aer/calc/` holds `statements.py`, `ratios.py`, `quality.py`, `wacc.py`, `dcf.py`,
`comps.py`, `fx.py`, `prices.py` and `bridge.py` — built through Phase 3, each with unit and
property tests. The production workflow is still `vertical_slice_v1`, and its `calculate`
step computes exactly one number: revenue CAGR.

```
{"job_id": "…", "count": 1, "names": ["cagr"], "event": "calculations.persisted"}
```

Everything else follows from that. The valuation page is empty because no run has ever
produced a discounted cash flow. The comparables page is empty for the same reason. The
balance-sheet, cash-flow, earnings-quality and DCF sections have one figure between them to
write from, so they write almost nothing even when the writer is working.

The services were reachable from the API and the web — `services/valuation.py`,
`services/scenarios.py`, `services/assumptions.py` all have routes — and **no workflow step
ran them**. A wiring gap, not a missing-feature gap, and the highest-value work in the
system at the time it was written.

`aer.services.analysis` closed it for the statements, the ratio suite and the
earnings-quality signals: a two-year scene now persists more than ten calculations where it
persisted one. **The valuation is a different kind of gap and stays open** —
`inputs_from` refuses to run without a confirmed assumption for every driver and scalar,
and says why: a terminal growth rate this platform chose "would be its opinion presented as
the operator's". So a DCF needs an operator working through the assumptions page, or an
agent that proposes assumptions — and that is a new role, which ADR 0035 says needs an ADR
before it needs code.

**That ADR is now written (0046) and two thirds of the answer is built.** Six of the eight
assumptions have a history in the filings the run already holds, so
`aer.services.assumption_proposals` derives them arithmetically with their basis stated.
The two that no filing answers — the perpetual growth rate and the exit multiple — come
from the `assumption_proposal` role, whose output contract has a field for each and no
other fields, whose bounds are checked in code, and whose out-of-band value is *refused*
rather than clamped. What remains for B2 is the gate that shows an operator every proposed
value with its justification, and the workflow step that runs the valuation once they have
confirmed them.

---

## List A — backend, safety, security, optimisation, operations

### Correctness and invariants

| # | Gap | Notes |
|---|---|---|
| ~~A1~~ | ~~**The workflow does not invoke the calculation suite**~~ | **Closed.** `aer.services.analysis` assembles statements, ratios and quality signals for each annual period into one ledger, and `calculate` runs it. One calculation a run became more than ten on a two-year scene. The DCF stays out deliberately: it refuses to run without confirmed assumptions, and an agent that proposed them would be a new role needing an ADR (0035). |
| ~~A2~~ | ~~**Unmetered model spend**~~ | **Closed.** The schema now goes to the wire as a dict, so the SDK sends it and skips the client-side parse: the stream always completes, the usage is always whole, and validation happens where a failure is a value already metered. `SpentButUnusableError` carries the bill and both payloads; `Agent.run` writes the `agent_runs` and `costs` rows before re-raising, with `stop_reason` marking them. |
| ~~A3~~ | ~~**The companyfacts quarantine**~~ | **Closed, and recorded as ADR 0044.** The aggregate takes the date of its newest component — the day it could first have existed — at 0.9 confidence to mark it derived. A current run gets a citable primary source; a historical run still quarantines it, now for a reason that is true. A side effect worth noting: the validator's date-adjudication call has disappeared from every run, because there is nothing left to adjudicate. |
| ~~A21~~ | ~~**A filing is fetched and only lightly read**~~ | **Closed, deterministically.** Forty paragraphs in document order is the cover page, the listing table and the transfer agent's address — all genuinely in the artefact, none of it citable by a research section. Two passes replace it: the document is cut at its statutory item headings (`Item 1`, `Item 1A`, `Item 7`, where the form *obliges* the prose to be), then paragraphs inside those items are scored on the vocabulary a research report uses and the best forty kept, emitted in the filing's own order. No model call, so the selection replays. A filing with no recognisable headings is read whole rather than not at all. |
| ~~A4~~ | ~~**One source per run**~~ | **Closed at the chosen scope.** `aer.services.filings` fetches the latest annual report and the recent current reports from the submissions index, inside the point-in-time window, each dated by its acceptance and excerpted so it can be cited. SEC full-text search, issuer-IR discovery and Companies House remain uncalled — see B13. |
| ~~A22~~ | ~~**The monthly budget cap was never enforced**~~ | **Closed.** `BudgetGuard` has carried `monthly_cap_gbp` since the engine was written, `aer.services.runs` populated it from settings on every run, its docstring said "two ceilings, and both matter" — and `check` read only the per-run one. The field was dead, and `costs` even carried an index commented "the monthly-budget query" that nothing ran. Against invariant 6 this is the worst shape a cap can have: not one that only warns, but one that does not warn either, so an £80 month held a £2.50 request thirty-two times over in silence. `spend_this_month` now sums the calendar month in **UTC** — the boundary has to agree with `occurred_at`, or the cap resets at a different instant depending on where the operator is standing — and deliberately **does not join** to the job, because `costs.job_id` is `SET NULL` precisely so deleting a request cannot erase what it cost, and a join would hand that escape straight back. The refusal names its `scope`, and the console says so: raising a request's own cap releases a per-run stop and does nothing at all to a monthly one. |
| ~~A23~~ | ~~**Two of the three call paths composed their own user turn**~~ | **Closed.** A leftover from A14, in the place A14 did not look. `Agent.run` was taught to send `stable_context` as a separate cached block; `run_batch` and `estimate_input_tokens` each built `Message(role="user", …)` by hand and predate it, so both omitted the prefix. For the batch path that is worse than a miscount — the prompt actually **sent** has its evidence missing, and the input-cap check under-counts to match, so nothing raises and a model is simply asked to write from nothing. Inert today only because the one role with a stable context does not go through the batch path, which is not a property anything enforced. `run` and `run_batch` now share `Agent.compose_turn`, and a test parses `base.py` and asserts a user turn is constructed in exactly one place, because the real invariant is "there is one constructor" rather than "these two happen to agree". `estimate_input_tokens` was **deleted** rather than fixed: it had the same omission, no callers anywhere, and a docstring claiming the approval gate and the budget guard used it when the guard reads each step's declared `estimated_cost_gbp`. Dead code carrying a false claim about a live control is worse than either on its own. |

### Security

| # | Gap | Notes |
|---|---|---|
| A5 | **No authentication** | `get_current_user` returns the first row of `users`. Correct for a local single-user tool; blocking for anything reachable from a network. Phase 6 keeps it behind a flag. |
| ~~A6~~ | ~~**The FRED API key is compromised**~~ | **Closed 2026-08-09.** ADR 0033. The operator rotated the key locally; the old one is dead. No code change was ever going to fix this one. |
| A7 | **No inbound rate limiting** | The token bucket protects outbound fetches. The web application has none. |
| A8 | **No production deployment story** | No `docker-compose.prod.yml`, no TLS or reverse-proxy configuration, no deployment guide. |

### Data lifecycle

| # | Gap | Notes |
|---|---|---|
| ~~A9~~ | ~~**No retention policy, no GC, no integrity sweep**~~ | **Closed, with a correction to the original claim.** `services/retention.py` did exist and did have no caller — but what it held was the *licensed purge* path, which answers a publisher's demand to destroy copies and is correctly idle while nothing licensed is in the store. The two sweeps a single-machine platform actually needs each week were simply absent, and are now there: `verify_store` re-reads every artefact and checks it still hashes to its name, and `collect_garbage` clears bytes no source document, report or agent run points at. `aer verify-artefacts` exits non-zero on a bad store so it can be a cron line; `aer gc-artefacts` reports and only deletes with `--delete`. Two traps closed on the way: a purged artefact is *expected* to be absent and is skipped rather than reported as loss, and every reference branch filters its nulls — `x NOT IN (…, NULL)` is never true, so one agent run with no archived response payload would have made the sweep return no orphans at all and look exactly like a clean store. |
| ~~A10~~ | ~~**No backups**~~ | **Closed.** `aer backup --to DIR` writes a `pg_dump` and the artefact store into one directory with a manifest hashing both — both halves or neither, since a database restored beside an empty store is a set of citations into nothing. A backup nobody has read is not a backup, so `aer verify-backup` re-hashes the dump and every archived file against the manifest and touches no database; it can run wherever the backup lives, which is where a restore is first attempted. `aer backup` runs it before reporting success and `aer restore` refuses without it. Credentials go to `pg_dump` through `PGPASSWORD`, never argv, because `ps` is world-readable — two tests hold that. The restore test restores into a scratch database and reads the rows back, so the source cannot be what it is reading. |
| ~~A11~~ | ~~**No audit-chain verification command**~~ | **Closed.** `aer verify-audit` walks the log in id order and stops at the first break, naming the event id so an operator can go and look at the row. Three things it had to get right: the log is paged, and the seam between two pages is the one place a break is invisible — the first record of a page has no predecessor in its own sequence — so `find_chain_break` now takes an anchor and the service carries it across; deleting the *beginning* of the log leaves every survivor self-consistent, so the first row is checked for a `prev_hash` it should not have; and an empty log reports as empty rather than sound, because "the chain is intact" printed against a truncated table is reassuring at the worst possible moment. Sabotage caught the page-seam test not testing the seam — deleting a row shifts every later row up a place. |
| ~~A12~~ | ~~**No run-level replay**~~ | **Closed.** `aer replay-run <job-id>` re-derives a run from its own record on four legs, each able to fail it alone: calculations re-execute and match, citations still find their excerpts in the artefacts, artefacts still read back by hash, and every model call still has both halves of its exchange archived. Nothing is fetched and no model is called — reproduction is a question about the record, not about the world, so it costs nothing and answers the same in a year. The fourth leg is the one that looks optional: an `agent_run` whose archived response has gone leaves prose in the report with no accounting behind it. Sabotage found the artefact leg was only passing because deleting a cited filing also fails the citation check; the test now uses an artefact nothing quotes, so only that leg can be the cause. |

### Observability and cost

| # | Gap | Notes |
|---|---|---|
| ~~A13~~ | ~~**No Langfuse, no OTel/Grafana**~~ | **Closed at the achievable scope — ADR 0049.** OpenTelemetry spans at the two levels a run is actually shaped by: `step.<key>` in the engine and `model.<role>` around the provider call. The exporter is off unless `AER_OTEL_ENDPOINT` is set — no endpoint, no imports, no thread, no connection — because a local-first tool that needs a collector before it starts is one nobody starts. Langfuse was declined: better suited to LLM tracing, but a vendor dependency unreachable from this environment, and an integration nobody can run is one nobody can trust. The property the tests are mostly about is that tracing can never fail a run; a tracer that raises on open, an exporter that raises on close and a bad endpoint all degrade to a warning. One real bug on the way: the obvious single-`try` shape can make the generator yield twice, which `contextlib` turns into a failure of the traced code. **Not exercised against a real collector** — verified through an in-memory exporter — and fetch spans are left until there is somewhere to look at them. |
| ~~A14~~ | ~~**No prompt caching**~~ | **Closed — ADR 0048, with the premise corrected.** Everything downstream already existed: `Usage` carries the cache token fields, `_usage_from` reads them, `costs.py` prices a read at a tenth and a write at a quarter more. Two findings changed the fix. The shared system prefix is *too short to cache* — `PLATFORM_CONTRACT` is ~183 tokens against minimums of 512 (Opus 5), 1024 (Sonnet 5) and 4096 (Haiku 4.5) — so ordering it first was never going to be enough. And the tokens are in the user turn, behind per-section text; caching is a strict prefix match, so no marker helps until the order changes. `Agent.stable_context` now declares the repeating head of a turn and the provider sends it as its own block ahead of what varies. The section writer puts the evidence there: the nineteen built-in sections resolve to a handful of evidence policies, so sections sharing one get a byte-identical block, and every retry gets it again. The trap was the token counters — both count only `content`, and leaving the prefix out would have silently doubled every role's input cap. **The saving is not yet measured**; no live run has been made since. |
| ~~A15~~ | ~~**No cost dashboard, no cache-hit measurement**~~ | **Closed.** `aer.services.spend` totals what a run or the platform spent and splits prompt tokens by how they were charged; `/costs` shows it with a per-role breakdown and the cache-hit rate. Two distinctions the page exists to keep: `input_tokens` from the API is the *uncached remainder*, not the whole prompt, so a summary reporting it as "prompt tokens" would make a heavily cached run look cheap — the total is fresh + read + written. And a hit rate of zero is not the same as no calls yet: the first is a defect and gets a banner naming the likely causes, the second is a dash. Money comes from the `costs` rows as metered, never recomputed, so a price change does not rewrite history. |

### Test-suite health

| # | Gap | Notes |
|---|---|---|
| ~~A16~~ | ~~**`tests/test_extraction.py` deadlocks in a full-suite run**~~ | **Closed — ADR 0047.** One line of test code, not a threading interaction: the memory-cap test called the child-only `_apply_memory_cap(1 << 30)` in the *pytest* process, and it sets `RLIMIT_AS` soft **and hard**, which an unprivileged process can never raise again. The session was capped at 1 GiB permanently; once its address space passed that (`VmSize: 1225312 kB` by the sandbox tests) every `mmap` failed, so `pthread_create` could not get a stack — `start_new_thread` returned, `/proc` showed `Threads: 1`, and `Thread.start()` blocked for ever. It wedged one statement *before* `_run_child` armed its parse timeout, so a memory limit presented as an unkillable hang. The assertion now runs in a subprocess and also checks the cap took effect. Worth recording that an earlier fix attempt blamed OpenBLAS and looked convincing on a two-file reproduction — it was only keeping that short run under the cap. |
| ~~A17~~ | ~~**DB-touching CLI tests contend for locks**~~ | **Closed.** `tests/db_cleanup.py` empties the database by deleting in reverse `Base.metadata.sorted_tables` order — dependency-sorted parents-first, so reversed is a safe deletion order — instead of taking the table lock a `TRUNCATE` needs. The transactional fixtures and a command that opens its own engine can now both be in flight without deadlocking. One trap found while writing it: the first version also deleted the reference data the migrations seed, and the twelve failures that caused surfaced in `test_section_writer.py`, a file with no visible connection to the change. `SEEDED_BY_MIGRATIONS` names `section_definitions` and `sector_profiles` and leaves them alone. |
| ~~A18~~ | ~~**The fake provider does not enforce schemas**~~ | **Closed, and it immediately earned its keep.** `FakeProvider` now refuses what the API would refuse: an unknown effort level, an unsendable request, and — through `_validated` — a scripted reply the declared contract does not permit, serialised and re-validated exactly as a real reply is. `ScriptedResponse(..., unchecked=True)` is the named, per-response opt-out for the one test that must inject an impossible reply to exercise a consumer's own defences. `tests/schema_guard.py` and `tests/test_fake_fidelity.py` hold it. **The first thing it caught was a production bug**, not a test bug: a skill declaring `{"type": "number"}` gets a float once the reply is validated into the pinned contract, so `8` arrives as `8.0` while the claim carrying its lineage reads "8 years" — and `unsourced_numerals` compared spellings, refusing a properly sourced section. `aer.providers.anthropic` had always validated this way; only the fake had been passing scripted ints through untouched, so no test could see it. `numerals_in` now canonicalises trailing fractional zeros on both sides. |

| ~~A24~~ | ~~**Nothing had ever tried to break the invariants on purpose**~~ | **Closed as an exercise, and it paid.** Thirty-six mutations, each breaking one of the eight invariants in `CLAUDE.md` the way a careless edit would, each run against the tests meant to notice. Thirty-one were caught. Of the five that were not, one is an equivalent mutant — making unit-symbol comparison case-insensitive changes nothing, because `_KNOWN_SYMBOLS` is lowercase and currency codes are `[A-Z]{3}`, so no two constructible symbols differ only in case. The other four were real holes, and none of them was a missing test file; each was a test that stopped one step short. **Invariant 3:** adding `source_document_id` to `_FIGURE_NAMING_KEYS` passed everything, though the code comment says it must never be there — it is the plausible widening that empties the rule, since a section citing a filing *feels* sourced. **Invariant 5:** `money + ratio` was tested and `ratio + money` was not, so a guard reading "is *this* one just a number?" refused the tested order and waved the other through — and the dimensionless left operand is the likelier one to write. **Invariant 6:** the per-run cap's *accumulation* was untested; every existing budget test set a cap below the first step's cost, so dropping `already` from the sum broke nothing. **B6/B11:** `_is_secret`'s bare-annotation branch is dead code today, because every credential is declared `SecretStr \| None` — so it could be deleted with nothing failing, and the next credential added as a plain required `SecretStr` would be silently editable. All four now have tests, and each was re-broken afterwards to watch the new test fail. |
| ~~A25~~ | ~~**A flaky test, hidden by the size of the suite**~~ | **Closed.** `test_every_agent_role_resolves_in_the_registry` walks `Agent.__subclasses__()`, and `test_agent_registry` defines throwaway subclasses that are *meant* to be invalid — one names a role nobody registered, to prove construction refuses it. `pytest.raises` holds the traceback that holds the frame that holds the class, so it stays in `__subclasses__()` until a **cyclic** collection reaps it, which is allocation-driven and not deterministic. Run those two files alone and it fails four times in five; run the whole suite and the hundreds of tests in between reliably collect it first, which is why 4214 green tests never showed it. The walk now excludes classes defined under `tests.`, which is also the more honest statement of the property: it is the platform's agents that must name a registered role. Worth recording that the first attempt to reproduce this *passed* and nearly had me call the fix a phantom — five runs, not one, is what settled it. |

| ~~A26~~ | ~~**The correctness core had never been attacked**~~ | **Closed, and it was the sweep worth running.** A24 aimed at the invariants and touched only `units.py` inside `src/aer/calc/` — which is the code the *first* rule in `CLAUDE.md` is about, and 8,400 lines of it. Fifty-seven mutations across all eleven modules: sign flips, off-by-one period indices, forgotten averages, swapped weights, inverted quotients, guards relaxed by one comparison. **Fifty were caught**, including every one that would have mattered most — off-by-one discounting, Gordon's numerator and denominator, present value dividing instead of multiplying, terminal value dropped from the enterprise value, CAPM adding beta instead of multiplying by it, swapped capital weights, population-versus-sample variance, and every sign in the margin bridge (the module names that as the easiest thing in it to get wrong, and it was right to). **Four escaped**, and each was re-run against every test file that can *reach* the module — 56 to 60 of them — before being called a hole: `working_capital` netting the wrong way; `_median` not sorting; the `abs()` gone from `rate_change`; a **nil** terminal cash flow admitted where a negative one is refused. All four now have tests, each re-broken afterwards to watch the new test fail. |
| ~~A27~~ | ~~**A calculation on the valuation's input path with no test at all**~~ | **Closed.** `working_capital` is exported, is *not* in `RATIO_DEFINITIONS`, and was called by no test — every apparent reference was `working_capital_intensity` or `opening_working_capital`, which are different things. `compute_ratios` never reaches it, so the ratio suite's coverage said nothing about it. Its one caller is `aer.services.valuation_run`, where it becomes the DCF's `opening_working_capital` — so the addition-instead-of-subtraction that survived the sweep would not have shown up as a wrong ratio on a page anybody checks. It would have shown up as a wrong first-year free cash flow, and from there as a wrong valuation, carrying a complete provenance trail the whole way. Worth stating plainly, because it is the shape of failure this platform is built to prevent: **every mechanism worked and the number was wrong.** |

| ~~A28~~ | ~~**A25 was not the only order-dependent test**~~ | **Closed, and the check is now a recipe rather than a one-off.** Reversing the file order found nothing — and that result was worth less than it looked, because reverse order happens to put `test_injection.py` *before* `test_agent_registry.py`, which is the safe order for A25. It would not have caught the one failure already known about. A shuffled ordering (seed 20260811) found two more: `tests/test_request_api.py` commits an `Artefact` through `_leave_evidence_behind`, and its `clean_slate` fixture truncated `research_requests, audit_events, users` — three tables, not the fourth. Source documents cascade from a request; an artefact is content-addressed and belongs to no request, so the row outlived the file. Two tests in `test_source_documents.py` then failed, because they asked whether the artefacts table held exactly one row and no rows respectively, which are claims about every test that ever committed rather than about the code under test. Both halves are fixed — the fixture now names `artefacts` (verified: one leaked row becomes none), and the two assertions were narrowed to the digest they created and to a count either side of the call. `just test-shuffled [seed]` runs any ordering and prints the seed, so a red run is reproducible. **Seen once more on 2026-08-20, from the other direction.** A new test class drove a real run to the assumptions gate — which commits a company, its facts, its assumptions and its job rows — behind a fixture truncating six tables named by hand. The two assumption modules that follow it then failed: one on the duplicate `(MSFT, NASDAQ)` listing, the other counting the leaked rows as its own proposals. The recipe did not fail; the ordinary gate caught it in default order. The narrower lesson is about the fixture: a test that drives the workflow commits more than any hand-maintained table list will keep up with, so it empties everything through `delete_all`, before and after. |
| ~~A29~~ | ~~**The first Windows run failed seven ways, one of them in production code**~~ | **Closed — every one found by the operator's own `just test`, none findable from Linux.** The production bug: `archive_request` formatted its refusal date with `%-d`, a glibc extension that Windows `strftime` rejects, so archiving a request twice — a guarded path, with a test, green on every Linux run — returned a 500 where the design says 409. Fixed with `.day`, and `tests/test_smoke.py` now scans every source line for glibc-only codes in `strftime` calls and f-string format specs, because a Linux suite can never catch this at runtime and reading the code is the only test that holds. The other six were portability holes in tests: three read `/proc/self/task` (Linux-only; now skipped off Linux, with the pin itself still tested everywhere), one needed `ZoneInfo("Pacific/Auckland")` and Windows ships no tz database (now a fixed 13-hour offset, same reader, no dependency), one ran the POSIX memory-cap child on a platform with no `resource` module (now skipped on Windows, whose branch has its own test), and fixing the fixture around the last one surfaced **a further order-dependence A28's three passes had missed**: the budget console tests created a fresh user per run, the console shows a run only to the *oldest* user, and a user leaked by `test_request_api` — which cleans at setup, serving the next test rather than itself — became the current user, turning a correct ownership check into a 404. The fixture now clears the slate on both sides. The wider lesson joins A26's: the suite had been green on one operating system the way it had been green in one file order. |

### Findings from the first full live run

*The four acceptance runs are the first exercise of the whole spine against the real API.
Everything below was invisible to the suite for the same reason: the fake provider is an
alternative implementation of the protocol, so it never sees the wire payload at all, and
the two roles that use the batch endpoint had only ever been run against it.*

| # | Gap | Notes |
|---|---|---|
| ~~A30~~ | ~~**The batch path sent a deprecated field, and found out an hour later**~~ | **Closed.** `red_team` failed a live AAPL run with `output_format: This field is deprecated. Use 'output_config.format' instead.` The single-call path had always been right and looked identical: on `messages.stream` the SDK *merges* the `output_format` argument into `output_config` before sending (there is a comment saying so in its own source), so the argument is client-side sugar. The batch endpoint takes raw message params and merges nothing, so the same spelling written into a batch request went on the wire as the deprecated field. It failed late as well as silently — the Batches API validates at result-fetch time, not at submission, so the step ran to completion and then returned "item 0 did not succeed (errored)". The fix merges into `output_config` with `setdefault` rather than assigning, because assigning would have dropped the `effort` key `_request_payload` had already put there and quietly bought a cheaper model call. The test that would have caught it asks the SDK rather than enumerating by hand: `batch_create_params.Request["params"]` *is* `MessageCreateParamsNonStreaming`, and the batch params must be a subset of what it declares — which has no `output_format` member, and will not have the next removed field either. |
| ~~A31~~ | ~~**A role that thinks hard with no room left to answer**~~ | **Closed.** Five of one report's sections came back with `stop_reason: max_tokens` and no draft. `report_writer` was capped at 8,192 output tokens, which is generous for a section of prose and is the wrong number to have chosen from: `max_tokens` bounds thinking and visible output *together*, and this role routes to opus at high effort, so a section that needed reasoning spent its whole allowance reaching a view and had nothing left to write it down with. The planner and the red team already carried 16,384 for exactly this reason; nothing connected the routing table to the registry, so the roles with headroom had it because somebody thought of it, and the roles without it were the ones that had not been run yet. `report_writer` and `assumption_proposal` — the other high-effort role under the old figure, and the two numbers a whole valuation rests on — are now at 16,384, and a test asserts the rule against the **default** routes: hard effort implies the floor. Asserted rather than enforced at import, because routes are operator-overridable through `AER_MODEL_ROUTES` and a configuration edit must never stop the package importing. |
| ~~A33~~ | ~~**The most expensive step in the run was the one the cap could not see, and the cap was wrong anyway**~~ | **Closed — ADR 0052. Found by following A31 rather than by looking for it.** Both budget-check sites in the engine are written `if step.estimated_cost_gbp > 0`, so an estimate is not advisory — it is the switch deciding whether a step is looked at. Every spending step declared one except `draft`, which is one Opus call per model-written section and measured **£5.17** on the live run: the single largest item in the workflow, waved through unchecked, and missing from the projected cost the operator is shown at the plan gate. **The consequence was not hypothetical.** The default per-run ceiling was £2.50; the operator's AAPL run went past eight pounds and was never paused, because the item that took it there was invisible to the thing whose job was to notice. That is A22's shape again — a ceiling that exists and is never compared — and it surfaced only because raising `report_writer`'s output ceiling raised the worst case of exactly that step, which is the question a change like that should be made to answer. The fix is in three parts: `draft` declares £6.00 (above measured, generous on the same principle as every estimate there); the default per-run budget moves to £12.00, because £2.50 predated any run to measure and would now stop *every* run at drafting — the same wrong number failing loudly rather than silently; and the test fixtures read that default from `Settings` instead of restating it, since a fixture budgeting more generously than production can never notice a step outgrowing the real ceiling. The rule is tested as a decision, not a measurement: every step is either guarded or named as deterministic, so a new step lands in neither list and fails with its own name. **What this does not fix:** the guard is still consulted only *before* a step, so nineteen sections inside one step run with no check between them. Recorded rather than fixed — per-call budgeting is a larger change than the one that found it, and needs its own decision about what a half-written report is worth. |
| ~~A34~~ | ~~**The research workers had a token allowance a real company outgrew**~~ | **Closed — ADR 0053, by operator direction.** The second live run failed at the analysis step: a worker composed 40,367 input tokens against the role's registered allowance of 30,000, and the refusal's own message misdiagnosed it ("something was included that this role is not meant to carry"). Nothing improper was carried — evidence digests accumulate across a worker's rounds by design, and a fourteen-thousand-fact company legitimately composes turns the fixture company never did. The call it refused would have cost about £0.20 against a £12 budget. The operator's direction was that tokens should never be the cap, and the fix honours it structurally: the per-role input allowances are **deleted from the registry**, not resized, and every call is instead priced before it is made — counted input at the uncached rate plus the full output ceiling — and checked against what remains of the request's budget and the month's, raising the same `BudgetExceededError` the engine already pauses on. That closes the gap ADR 0052 left open: the calls inside a step are now each guarded, so the nineteenth drafting call is checked against everything the first eighteen spent. One token-shaped check survives because it is the vendor's — a composition that cannot fit the routed model's context window is refused before upload rather than 400'd after it. What the old allowances quietly provided as interpolation tripwires is given up knowingly: that failure now surfaces as bounded, visible cost rather than a named refusal, and the trade is recorded in the ADR rather than implied. |
| ~~A35~~ | ~~**Half a report's sections lost to a length nobody told the model about**~~ | **Closed.** The second live run drafted six sections and lost three: `historical_financial_analysis` came back with 22 claim statements over the 600-character bound, `balance_sheet_liquidity` with 20, `management_governance` with 9 — each burning both its attempts. `max_length` reaches the model as *description text*, because the API's schema mode rejects the constraint itself, so it is guidance rather than a rule the server applies: a structurally perfect reply overruns it, is paid for, and is thrown away. **The planner had already been fixed this way** (`TestThePlannerAsksForWhatItValidates`, after a 660-character focus killed a call), and the section writer inherited its bounds without the lesson — the same defect, in the same shape, one module along. Now: `CLAIM_STATEMENT_BUDGET`/`CEILING` and their `BASIS` pair sit beside the field they bound, the ceiling is well clear of the budget so ordinary variance costs nothing, and both drafting prompts interpolate the budgets from those constants so instruction and validation cannot drift. **The second half was worse than the first.** The retry loops handed the model the exception's own message — *"22 field(s) broke a constraint the API does not enforce"* — which is true, unactionable, and exactly why both attempts failed identically. The field-level detail was in the exception's context the whole time and only the research worker was reading it; `schema_problems` is now shared and both other loops call it. Mutation found the gap in the first version of the test, too: reverting a call site left the helper correct, tested, and unreached, so the property is now asserted of the loops rather than of the function. **The AMZN run then reproduced the whole finding on the red team before the fix landed**: six challenge statements and the coverage note over the same unstated 600-character bounds — and on the batch path there is no retry, so the one unreadable reply failed the whole step and with it the run. `RedTeamChallenge` and `RedTeamReport` now carry the same budget/ceiling constants, rule 6 of the red team's prompt states them, and the same mutation pair (ceiling to budget; rule deleted) fails the new tests by name. |
| ~~A36~~ | ~~**A failed batch reply is billed but never metered**~~ | **Closed.** The failed AMZN red team showed it: the batch completed at the API — tokens billed — and then failed validation here, and the raise was a plain `ValidationError` carrying no usage, so `run_batch` wrote no cost row for money that moved. Invariant 6's own words applied: spend the meter cannot see is spend the cap cannot cap. The batch path now inherits the single-call fix it predated: success at the API is settled for every item first, and a reply that then fails validation raises `SpentButUnusableError` carrying the **whole batch's** summed usage — the sibling items were billed too, and metering only the failed item's share would hide most of the money — with the failed item's archived exchange, and `run_batch` meters it through the same `_meter_a_failure` the single path uses before letting the error continue. Mutation-verified both sides of the boundary: a `run_batch` that re-raises without metering, and a failure carrying only the failed item's share, each fail a named test. **What this does not cover, recorded honestly:** an item that *errors at the API* still fails the whole call as an `ExternalServiceError` with no usage attached, so succeeded siblings of an errored item remain unmetered. The API does not bill errored items themselves, the case has appeared only when every item failed identically (A30's deprecated field), and carrying usage on a transport error is a muddier change than this one — noted here rather than half-done. |
| ~~A37~~ | ~~**One uncited finding out of ten killed a whole run**~~ | **Closed.** The third live run died at `research_recent_developments`: the worker's final report carried nine findings on named evidence and one that cited nothing, the finding validator refused the tenth — correctly — and with the rounds spent the step raised `worker_exhausted`, abandoning every sibling step two minutes into a fresh run. The prompt was not the problem this time (rule 2 states the citation requirement, with "make it a lead" as the named escape), and the field-level feedback worked; the model simply slipped once at the worst moment, and the failure's remedy — *leave that finding out* — was a deletion code could perform and did not. It does now: `_salvaged_turn` re-reads the billed reply from the exception's archived payload, drops every finding that fails its own validation, and accepts the remainder only if the whole turn then validates — a pure narrowing of model output, which is code's decision to make (ADR 0036's direction), performed for free from a reply already paid for and archived. Salvage declines whenever it cannot repair (no payload, unparsable text, findings not the fault, still invalid after the filter), landing on the existing say-it-back path, so it can save a round but never invent one; and what survives still faces the caller's validator over the run's real evidence, so salvage repairs shape without vouching for ids. Discards are logged at warning as `worker.findings_salvaged`. Mutation: never-salvage and keep-the-inadmissible each fail their own test; the explicit tool-request guard proved an equivalent mutant — `WorkerTurn`'s own validator already refuses that shape — and was deleted rather than kept as decoration, with the test retained as the pin. |
| ~~A39~~ | ~~**Every section was starved while the run held the evidence**~~ | **Closed — ADR 0055.** The first complete report held no revenue figure in prose while page 21 plotted four years of it from stored facts. Three selection defects compounded: forty facts chosen newest-period-then-*alphabetically* (18,588 facts reduce to "Accrued…, Accumulated…, AvailableForSale…" and Revenue never survives to R), excerpts chosen oldest-first (the same signature page delivered to fourteen sections), and compact listings admitted before excerpts so every excerpt was overflow. Selection is now the section's: facts rank by the section's canonical-concept preference then recency, excerpts by keyword affinity with a substance filter that refuses administrative furniture, pools far wider than the caps, and the listings held to a budget share so excerpts always keep a seat. The preferences are rows, not code: the first cut keyed them by section key and the hardcoded-key guard refused it, so migration 0029 merges `concept_priority` and `excerpt_keywords` into each seeded `evidence_policy`, `SectionPolicy` carries them, and the spine seed pin requires both — non-empty and canonical. Custom sections inherit the default ordering. Mutation-verified four ways, each failing a named test. |
| ~~A40~~ | ~~**The report was written to the operator, and its shape overclaimed its content**~~ | **Closed — ADR 0055, coverage decision the operator's.** The Executive Summary page printed eight raw validator lines with UUIDs; sections narrated evidence budgets, retrieval plans and re-run instructions; footnotes carried twelve-decimal ratios and the unit "pure"; literal `\u2014` appeared mid-sentence; a near-blank page preceded an unbreakable table; and nothing marked the four missing sections in the contents. Now: one coverage notice at the front, derived from recorded state (section statuses + failed evaluation rows) with a link to Sources — the operator's chosen posture for thin runs; failed sections render their status line only, with diagnostics kept to the run's console; the contents marks ungenerated sections; escapes are decoded at render; calculated values display at four decimal places marked when rounded, full precision stored; tables break by row; and both drafting prompts carry the reader-voice rule. Five mutants each fail a named test. |
| ~~A41~~ | ~~**The disagreements section was written one step before the disagreements**~~ | **Closed.** The live report said "No disagreements between sources were recorded" above eight recorded red-team challenges, two material — because `validate` fills the section and `red_team` records the challenges one step later, into a page already written. The red-team step now refills the deterministic VALIDATE sections after its challenges land and *before* the gate-2 payload is hashed, so what the operator approves carries the adversary's rows. The builders overwrite, so the refill is the same fill with a fuller denominator. Pinned twice: an overwrite test through a recorded challenge, and a source-order test (record, refill, seal) that fails if the refill moves or dies behind a raise. |
| ~~A42~~ | ~~**The planner's list bounds were never stated, and one extra risk killed a run**~~ | **Closed.** A live run died at step one: eleven `known_risks` against a schema bound of ten that rule 6 never mentioned, on the one call in the workflow with no retry — a £0.12 bill for a plan sound in every other respect. Rule 6 now states all three list bounds beside the character budgets ("order each list strongest first and stop at the bound"), the prompt version is bumped to 3, and code cuts an over-full list back to its bound from the billed reply's archived payload — a pure narrowing, head-first by the model's own ordering (ADR 0036) — revalidates, and sends the trimmed plan to gate 1 where the operator sees exactly what will run. Salvage declines whenever trimming is not the repair. Three mutants each fail a named test, including a wiring mutant that parked the salvage behind an early re-raise. |
| ~~A43~~ | ~~**A worker fetch re-recorded the run's own filings as quarantined T5 duplicates**~~ | **Closed.** The live run recorded Microsoft's FY26 10-K three times: once properly at T1 by the acquire step, then twice more — new ids, T5, quarantined for "no publication date" — each time a worker fetched a URL resolving to bytes the run already held. Competing ids for one document poison every citation of it and depressed `source_coverage`. The fetch executor now answers from the request's best existing record of the same artefact digest — highest tier, unquarantined first — and mints nothing. Matching is content-addressed, so a URL variant of the same document still resolves to the held record. Both mutants (lookup disabled; worst record answers instead of best) fail the named test, the second caught only after the test learned to seed the live failure's quarantined duplicate beside the T1 original. |
| ~~A44~~ | ~~**The reference exemption spelt "Form 10-K" while writers write "the 10-K"**~~ | **Closed, within ADR 0054's boundary.** The first live run after the exemption still lost a drafting retry to a bare `10-K` — the far more common spelling — and "Securities Exchange Act of 1934" still shed 1934. The pattern now covers the closed list of bare form types (10-K, 10-Q, 8-K, 20-F, 6-K, S-1 and kin, with /A variants) anchored by their letters, and a statute's year anchored by "Act of". Deliberately a closed list, so "a 10-4 split" and its neighbours stay figures — pinned by the same test that the mutation dropping the alternative fails. |
| ~~A32~~ | ~~**The numeral rule refuses document references**~~ | **Closed — ADR 0054, by operator decision.** The live run logged repeated `section_writer.draft_refused` over numerals that denote no quantity: the year `2026`, Apple's CIK `0000320193`, item and exhibit numbers such as `2.02`, `99.1` and `9.01` — the rule behaving exactly as §2.12 said it should, at the cost of retries at Opus prices and prose that writes around dates. The operator was offered three positions — keep it fully strict, exempt recognised reference patterns, or a broad year exemption — and chose the middle one; the broad exemption was declined as unsafe because year-shaped quantities exist. `without_document_references` now erases recognised reference spans from content before the numeral scan: anchored dates and fiscal markers, a year in temporal company, labelled filing references, a labelled CIK, an accession number. The exemption is by **span**, never by value — "Revenue grew in 2026 to $2,026 million" loses only the date and the money still needs lineage — and it applies to content only, never to the claims that provide cover, so it can only narrow what the scan flags. A bare unanchored year still trips, and the writers' prompts now carry the remedy: anchor every year to a month, a quarter or a temporal word. This is `NUMERAL_EXEMPT_KEYS`' own reasoning, one level up — provenance must not trip the rule that exists to protect provenance — and it moved invariant 3's boundary, which is why it waited for an ADR and the operator rather than being patched. Mutation-verified: an eraser that erases nothing, an eraser applied to the claims as well, and a broadened bare-year exemption each fail a named test. |
| ~~A38~~ | ~~**A validator refusal on the final round was fatal, with the fix in hand**~~ | **Closed.** The fourth live run died at `technical_context`: the final report's finding 3 cited SEC accession numbers (`0001193125-11-282113`) where UUID `source_document_id`s belong, and the validator refused it with the exact remedy in the message — but the refusal landed on round 5 of 5, so the feedback had no round left to run in, and three sound findings died with the run while every sibling worker finished. A37's salvage did not apply: that repairs replies the *schema* cannot read, and this reply was schema-perfect — it failed the run's own validator, one layer up. Now a validator refusal on the last round narrows instead of killing: `_narrowed_report` re-validates each finding alone, keeps only the clean ones, and validates the survivors **together** before accepting — asking the validator rather than parsing its messages, so a report-level problem refuses every singleton and lands on the unchanged exhaustion path. Feedback stays strictly preferred while a round remains to act on it; dropping is the last resort, logged at warning as `worker.findings_dropped_at_exhaustion`. Worker rule 2a also now names the trap — an accession number names a filing but is never its id. Mutation-verified four ways, each failing a named test: a rescue that never fires, a filter keeping the refused findings, survivors accepted without joint validation, and narrowing that pre-empts feedback. |

| ~~A45~~ | ~~**The annual-fact filter trusts a label it never checks**~~ | **Closed.** `fiscal_period` is EDGAR's `fp`, and `fp` describes the *filing*, not the fact: a 10-K files its cover-page instants and its fourth-quarter stubs under `FY` beside the twelve-month figures. So `dei:EntityCommonStockSharesOutstanding` — dated the day the filing was signed — minted a one-fact "annual period" a fortnight after every year end, and with only `MAX_PERIODS` taken the newest window filled with cover dates while the fiscal years fell out of it. Durations now decide which periods exist and instants join a period only when one does, so a balance sheet dated its own year end is kept and the furniture is not. A shorter duration ending on the year end is dropped rather than deduped against, because it ties with the annual figure on `(period_end, concept)` and a later-filed stub would win it — a quarter's revenue standing in for a year's, which is worse than the absence. Mutation-verified twice; the stub test was vacuous on its first writing (the stub sat outside the point-in-time window and never entered) and was corrected until it bit. |
| ~~A46~~ | ~~**Concept-map coverage is unmeasured, and thin where a forecast needs it**~~ | **Closed as measurement, and the "thin" half was mostly wrong.** Inspection found the map healthier than this note assumed: revenue carries six tags, and the canonical tags for operating income, capex, current assets and liabilities and tax were all present — so the map was not padded with guesses. Two spellings large filers use were added (`DepreciationAndAmortization`, `PaymentsToAcquireProductiveAssets`) and bare `Depreciation` deliberately left out, being a smaller number than the combined line. What was genuinely missing was the measurement: `AnalysisOutcome.forecast_coverage` counts the periods carrying each of the eight driver concepts, the analysis step records it on its output row, and the completion log names the thin ones — so a run says at `calculate` what the assumptions gate will ask for, instead of the operator discovering it after the money is spent. |

### Findings from the AAPL run of 2026-08-20

*The first run on the polished code, and the first to exercise the whole spine on a
company with real depth: 14,373 facts, 19 fiscal years, 176 calculations, 122 citations
all verified, £7.60 against a £12 cap. What it exposes is no longer correctness of the
guards — those held — but their **cost**. Thirteen of sixteen sections were drafted
twice, and the draft step is 76% of the run's spend and 62% of its compute.*

| # | Gap | Notes |
|---|---|---|
| ~~A48~~ | ~~**Bare years still trip the numeral rule, and nobody can see which span did it**~~ | Four sections were refused over `the numeral(s) 2025` / `2026` — earnings quality, key risks, scenarios, valuation — each costing a full Opus retry. ADR 0054 anticipated exactly this and chose the span eraser plus a prompt rule ("anchor every year to a month, a quarter or a temporal word") over the broad year exemption, which the operator declined as unsafe. **That decision is not being revisited here, because the evidence needed to revisit it does not exist:** the refusal names the *value* and never the surrounding text, so it is impossible to tell from the log whether `_REFERENCE` has a gap or the writer ignored the rule. The fix is therefore instrumentation first — carry ~40 characters of context around each flagged numeral into the refusal message and the log — and only then a pattern change, argued from spans the next run actually produces. Cheap, safe, and it converts a recurring cost into a decidable question. **The instrumentation is delivered; the pattern decision is not.** A refusal now quotes ~40 characters around the flagged numeral *from the erased text* — what the scan actually read, rather than what the draft said — so the next run names the anchor that failed instead of the value that tripped. Nothing about what is flagged has changed, which is the point: ADR 0054's decision stands until a live run produces spans to argue with. **The MTB run produced them, and the pattern decision is now made** (ADR 0054, amended 2026-08-20). Five span classes join the eraser, each argued from a quoted refusal: rankings ("a top-20 U.S. bank"), labelled file numbers ("Commission File Number 1-9861"), hypothetical rate steps ("a 100 basis-point increase" — the article and the compound are the anchors; "NIM fell 12 basis points" still needs lineage), statutory thresholds ("above the $100 billion asset threshold" — "$100 billion of deposits" does not ride), and the standalone zero ("0%" and "0.5" stay flagged). The discipline is unchanged — by span, never by value; content only, never claims — and every class carries a nearest-quantity counter-test. Mutation-verified five ways, all killed. |
| ~~A49~~ | ~~**Every section was drafted against a truncated evidence pack**~~ | `evidence_truncated: true` on all sixteen sections, from a corpus of **43 excerpts across 9 documents** — about five per filing, from a 1.5MB 10-K among them. That is the root cause of the report's thinnest pages: Business Overview could not restate Item 1, Segment Analysis had no segment footnote, and the executive summary spends its length explaining what it cannot say. A39 fixed *selection* (rank by the section's concepts, keep excerpts a seat); this is *supply* — the pack is well chosen from too little. Fix, in order: extract more excerpts per filing, targeted at Items 1, 1A and 7 and the segment and geography footnotes; then re-measure truncation, because a wider pool with the same cap changes what is cut. **Instrumented first, the same posture as A48:** the item-targeted extraction already exists (Items 1/1A/7 for a 10-K, sixty excerpts a document), so 43 across nine documents means the supply chain failed somewhere the log could not see — the extractor, the paragraph splitting or the ceiling. Each document's sweep now logs its character count, excerpt count against the ceiling and mean excerpt length, so the next live run says which stage starved it; widening blind before that answer would be guessing at the wrong stage. The artefact bytes live on the operator's machine, which is why this needs a run rather than a desk check. **Closed by the MTB run's reading.** The instrumentation answered on its first live outing: 703,420 characters → 9 excerpts, mean 36,003 characters, ceiling never bound — the paragraph split cut on blank lines and iXBRL text has almost none, so whole statutory items were recorded as single excerpts. The splitter now re-cuts any block past 2,000 characters at line breaks, then sentence ends, then spaces, every piece a verbatim locatable span; a filing with blank lines splits exactly as before. Truncation is to be re-measured on the next live run (A63 tracks the downstream section failures). |
| ~~A50~~ | ~~**Section output runs about ten times the word budget, and the overrun is paid for then thrown away**~~ | **Closed.** Cash Flow returned 14,475 output tokens against a 711-word budget; Key Risks 1,122 words against 812; Scenarios 977 against 711. Three sections were salvaged for length and two more retried. The budget is enforced *after* generation, so the run buys ten thousand words and keeps one. Fix: state the ceiling in the prompt with its consequence ("anything past the budget is cut, not returned") and interpolate the number from the same constant the validator reads, as A35 did for claim statements. **Do not simply cap `max_output_tokens`** — that is what killed Balance Sheet (A51). Delivered as written: the user message states the budget and the count past which the draft is refused or cut, both interpolated from the constants the validator itself reads, and says what the overrun costs — it is paid for and then thrown away. The output ceiling was left alone. |
| ~~A51~~ | ~~**Three sections were lost, each for a different and separately fixable reason**~~ | **Closed, in three separate fixes.** *Balance Sheet & Liquidity*: both attempts hit the output ceiling ("ran out of room before it was complete"). The retry says "say it in fewer words" but demands the same content, so the second attempt fails identically — A35's lesson, one loop along. Fix: a truncation refusal must cut the section's word budget for the retry, not merely ask. *Capital Allocation*: attempt 1 filed nineteen claims citing an extraction belonging to a different source document; attempt 2 over-corrected into numeric claims with no citation at all. Fix: hand the writer an opaque citation handle binding `(source, extraction)` as one token, so a mismatch is unrepresentable, and state both constraints together in the retry. *Valuation — DCF*: refused for naming a discount rate, risk-free rate, ERP and terminal growth that no calculation produced. The guard is right and the call was pointless. Fix: when no valuation exists, skip the model call and fill the section deterministically — the augmenter already writes the honest explanation the PDF carries. **All three delivered.** The truncation retry halves the word budget and says so; a valuation with nothing valued is filled from the record with no model call; and the citation handle went further than binding the pair — the proposal schema now carries the extraction id *alone* (`extra="forbid"` refuses a restated source outright), with the document resolved in code from the extraction's own record at recording time, so the mismatch that cost Capital Allocation nineteen claims cannot be expressed at all. |
| ~~A52~~ | ~~**The valuation never ran, and the gate let it through without saying so**~~ | **Closed.** `assumptions.gate_assembled derived=6 proposed_opinions=2 outstanding=3` — the cost-of-capital trio, unanswerable because no price feed means no beta regression. The operator approved, `value` completed in 259ms having produced nothing, and the consequence (no DCF, no scenarios, three omitted exhibits, one failed section) surfaced only in the finished PDF. **Amended on the operator's own report from the run:** the gate page has no control for saving an adjusted value or adding a missing assumption at all — the three were not skipped, they were *unenterable*, and the only actions the page offers are approve and reject. That upgrades the fix from a wording change to a missing surface. Fix: an entry form on the assumptions gate — adjust a proposed value, supply a missing one, each recorded through the assumptions service as a human-set value with the gate payload and hash refreshed — plus the consequence stated before approval: "3 of 11 outstanding: approving now produces no discounted cash flow, no scenario bridge and no sensitivity grid". Delivered in both halves. The gate page carries inline confirm, amend and supply forms that record through the assumptions service and return to the gate rather than stranding the operator elsewhere, with the consequence stated above the approve button. And the approval is verified against a hash recomputed from the *live rows*, not the proposing step's record: a row amended while the run waits re-opens the gate instead of resuming on figures nobody approved, while an untouched set reproduces the frozen payload byte for byte (ADR 0046, amended). |
| ~~A53~~ | ~~**The withheld-comps paragraph promises figures that were never computed**~~ | **Closed.** The report reads "A comparable-company analysis was performed against 1 peer(s) … with 4 proposed peer(s) excluded. **The figures are withheld from this version.** … available in full on the operator's own copy." The log says `comps.built peers=0 excluded=5` and `comps.table_built computed=0`: no multiple was computed, and there is nothing on the operator's copy either. Two defects — the counts disagree with the step's own record, and the withholding language describes an absence as a restriction. This is the platform saying something it cannot support, which is the failure it exists to prevent. Fix: give `WithheldComps` a distinct state for "nothing was computed" with its own sentence, and reconcile `peer_count`/`excluded_count` with what `comps_run` logged. Delivered: a zero peer count now gets its own paragraph — an analysis attempted, every proposed peer excluded for want of usable data, no fuller version withheld anywhere — and the comps outcome records `excluded_count` beside the grouped exclusions, so the sentence and the log cannot drift apart. |
| ~~A54~~ | ~~**Recorded calculations carry no period label**~~ | **Closed.** The red team found this unaided and scored it 3/5: the report prints FY2025 margins on its front page while the historical section says no FY2024/FY2025 ratio exists, and anchors its structural reading on FY2021 ratios nothing in the record dates. Every recorded ratio is period-less, so a reader cannot tell a current figure from one five years stale, and two mutually inconsistent margin sets sit side by side undated. Fix: carry `period_end`/`fiscal_year` on the calculation record and render it in the figure label and the footnote. The highest-value correctness fix after A49. **On inspection, half of this predated the run:** gap C1 (2026-08-17) already stamps every calculation struck inside a period scope — `period_label`/`period_start`/`period_end` on the row, the label carried into the section evidence and the fact-basis policy — so the storage and the writer's view were done. What no reader could see was the *rendered* half: the calculation footnote, where every figure resolves, never printed the stored label. It does now, in all three notations. |
| A55 | **175 concepts and 110 segment tags the map cannot place** | `analysis.completed unplaced=175` and `segments.swept unmapped_tags=110`. The commercial paper the red team cited as evidence is in the facts and absent from the assembled statements. It is also why the segment chart plots the "Product" aggregate beside its own components and prints "Wearables Homeand Accessories" — a missing space in a concept label. A46 built the measurement; this is the backlog it measures. Fix: work the unplaced list (the run already names it) into the concept map; suppress aggregates in the segment exhibit; fix the label spacing. Note the chart also contradicts the prose beside it — the exhibit builder had segment facts the section writer's pack did not — so the two should read one definition of what the run holds. **The code half is done:** a subtotal is now recognised by arithmetic — a member whose value equals, exactly, the sum of two or more of the others is a subtotal of them and is suppressed, which a *name* rule could never decide (a filer segmented into only Product and Service keeps both) — and the glued-conjunction labels are respaced from a table of the glued words themselves, because a pattern splitting every embedded "and" would mangle Ireland and England. **The concept-map curation remains open**: the 175 unplaced concepts and the tag backlog need the unplaced list worked entry by entry against the vocabulary, which is judgement over accounting semantics rather than a code change, and the chart-versus-prose reconciliation rides with A49's evidence-supply work. |
| ~~A56~~ | ~~**The same documents are fetched and re-parsed several times inside one run**~~ | **Closed.** `company_tickers_exchange.json` (520KB) fetched six times in three seconds during peer resolution, once per peer; the 1.5MB 10-K fetched at least six times during research and re-extracted each time, recomputing 96 hidden-text findings on every pass. The artefact store recognises the bytes (`artefact_was_new: false`), so the storage is deduplicated and the *work* is not. Fix: memoise fetch-and-extract within a run by URL, and let the tickers file be resolved once per run rather than once per peer. Costs nothing in correctness and returns time and SEC rate-limit headroom. Delivered on all three fronts: extraction is memoised by digest, a URL the run already holds is served from the archive without a refetch, and the tickers file is fetched once per client rather than once per peer. |
| ~~A57~~ | ~~**A worker restart replays every dead job and calls each one an error**~~ | **Closed.** Twenty stale queue entries — runs deleted by `reset-research`, jobs that outlived them — each failed with `ValidationError: No run <id>` and a full traceback, and arq retried them because the handler raises. Two minutes of startup noise in which a real failure would be invisible. Fix: treat a missing run as a benign discard — warn, return, do not raise — and have `reset-research` flush the queue it invalidates. Delivered: the existence check moved to the top of the handler, so nothing is built on behalf of a run that is gone, and answering with a discard rather than a raise is what stops arq retrying it. |
| ~~A58~~ | ~~**The injection heuristic flags every SEC filing it reads**~~ | **Closed in two halves, one of which predated the run by a day.** The flagging half was polish P9 (2026-08-19): on a document carrying `ix:` fact elements, the hidden-text and invisible-styling signals are marked *informational* — stored for the reviewer, never flagging the document, logged once as a count rather than as warnings — while every other signal, and every other document type, keeps full weight. The determination is made from the markup itself (`<ix:` present), not from anything a caller declares, so the downgrade cannot be bought by naming a content type. The recompute half — the same findings recalculated on every re-parse of the same bytes — was the A56 fix: a URL the run already holds is served from its own archive, and extraction is memoised by digest within a worker node. |
| ~~A59~~ | ~~**The research workers use EDGAR full-text search as if it were semantic**~~ | **Closed.** Roughly eight of twenty searches returned zero hits, all of them long natural-language phrases — `"foreign exchange currency headwinds macroeconomic conditions"`, `"antitrust App Store investigation Digital Markets Act"`, `"quarterly results fiscal 2026"`. EDGAR's index is exact-phrase. Fix: one line in the worker prompt saying so, with the remedy (short distinctive phrases, one concept at a time). The cheapest yield in this table. Delivered in the tool brief itself, which is interpolated into the system prompt — so the worker's prompt version moved with it. |
| ~~A60~~ | ~~**`presentation_integrity` failed and the report does not say which check**~~ | **Closed.** The coverage notice names the failed metric, which is right, but neither it nor the log names the finding, so the operator cannot act on it without opening the approval page. Suspects from this run: the segment chart contradicting its own section (A55), or the salvage artefact left mid-sentence in the executive summary ("filing furniture. as a California-chartered registrant"). Fix: carry the metric's own findings into the evaluation row and surface them where the failure is announced. Delivered on two surfaces: a "What the Failed Checks Found" table in the validation section (contract v3, migration 0050 — one row per finding, bounded at ten per metric with the remainder counted, and a failed check that recorded no findings still named), and a warning per failed metric in the worker log. Empty on a clean run, so a report with nothing to confess does not change shape. |

### Findings from the MTB run of 2026-08-20

*The first run on a company that is not shaped like the platform's assumptions. M&T is a
bank, and nearly every finding below is a bank walking into an industrial-company
assumption: a classified balance sheet that does not exist, a revenue caption that names
the wrong line, leverage measured without deposits. The guards all held — 91 citations
verified, every gate paused where it should, and the red team found the central defect
unaided with a severity-5 argument built from the platform's own recorded values. What
the gates did not do is stop an impossible number reaching the front page, because no
gate asks whether a number is possible. £7.89 spent; 11 of 18 sections generated.*

| # | Gap | Notes |
|---|---|---|
| ~~A61~~ | ~~**The front page publishes an impossible number, and every guard held while it did**~~ | Front page: Revenue Q2 FY2026 $442m, Net income $818m, Net margin FY2025 **172.1%**. Income exceeding revenue is arithmetically impossible on a consolidated statement, and the footnotes carry the whole contradiction in plain sight: net margin 0.31 for FY2021, then 1.31, 1.85, 1.68, 1.72. Invariant 3 held perfectly and did not help — $442m *is* a stored fact with a hashed source, 172.1% *is* a recorded calculation with formula, inputs and code version. The platform guarantees traceability, not sanity, and this run is the proof those are different properties. `numerical_consistency` passed at 0E-8 because it checks that a figure is reported *consistently*, not that it is *possible*. The red team caught it (severity 5/5, from the recorded values alone: "a margin above 1.0 means the denominator is not revenue but a partial caption") — but a red-team finding is advisory prose on the approval page, and the number still rendered. Fix: a deterministic plausibility layer — a small closed set of relations that cannot hold (net margin above 1 on a consolidated statement, net income above revenue, near-zero asset turnover on a nine-figure balance sheet), checked in code over the recorded calculations, each failure withholding the figure from rendered surfaces and raising an escalation trigger so approval happens knowing it. Never a model call: these are forty lines of arithmetic with unit tests. **Delivered (ADR 0066), in three layers.** The relations live in `aer/calc/plausibility.py` (pure, strict, property-tested); `figure_plausibility` joins the run-time metric set at threshold zero, so a failure reaches the validation table, the approval page, the check-failed log line and the VALIDATION_FAILURE banner through machinery that already existed; and the at-a-glance block re-checks the relations over exactly the rows it is about to render, withholding itself whole with the relations stated — ADR 0061's posture: told, rather than shown a lie. Which figure is wrong stays undecided, deliberately: income above revenue means one of the two is mislabelled, and saying which would be a guess (that is A62's fix). Mutation-verified eight ways, all killed — including a RUN_TIME assertion that was satisfying itself until the row's presence was pinned by name. |
| ~~A62~~ | ~~**"Revenue" resolves to a partial caption from FY2022 onward**~~ | The revenue history reads $5,992m (FY2021) → $1,525m → $1,484m → $1,541m → $1,657m. FY2021 is total revenue; everything after is a smaller line — plausibly a single fee caption — and the break lands exactly where M&T absorbed People's United (April 2022) and, more to the point, where the concept map's preferred revenue tag stopped being the one the filer used. A bank's income statement leads with `InterestAndDividendIncomeOperating` and `RevenuesNetOfInterestExpense`, neither of which the map carries, so the resolver fell through to whichever mapped tag still had values. Every downstream figure inherits the error: the 172.1% margin (A61), asset turnover of 0.0076, the segment chart totalling under $800m for a $219bn bank, and a base case framed on "expense growth below revenue growth". Fix, in two halves: add the bank income-statement captions to the concept vocabulary (net interest income, interest and dividend income, revenues net of interest expense, provision for credit losses), and prefer the filer's *total* concept over a component when both are present in a period — with a recorded fallback note when the map has to settle for a component, so the report can say which line it is calling revenue. **Delivered, in both halves.** The vocabulary gained the depository captions — `RevenuesNetOfInterestExpense` mapping to revenue, and four new canonicals (`net_interest_income`, `interest_and_dividend_income`, `noninterest_income`, `provision_for_credit_losses`) with seats in the income-statement layout; the CECL-era provision tag is deliberately absent until a live run's unplaced list names its exact spelling, per the module's nothing-guessed rule. And revenue selection is now a ranked decision rather than arrival order: `REVENUE_TAG_PREFERENCE` orders every tag that maps to revenue most-total-first, the companyfacts parser demotes a component to its own tag wherever a total exists for the same period (kept and visible, never dropped, per period not per document), and a component-only period still maps with `raw_concept` naming the line the platform settled for. A drift guard holds every revenue alias ranked — and caught the UK `Turnover` tags on its first run. What remains is the operator's next MTB run: existing stored facts predate the fix, and a re-acquisition is what re-labels the history. Mutation-verified six ways, all killed. |
| A63 | **Five failed sections are one failure: the starved pack, then a retry that swings past the target** | Industry, Historical Financial Analysis, Growth Outlook, Key Risks and Catalysts all died on "A numeric claim needs at least one proposed citation", and the sequence is legible in the log: attempt 1 cites extraction ids the section's evidence does not hold (the writer reaching for evidence it can see exists but was not dealt), the refusal says so, and attempt 2 over-corrects into numeric claims with no citations at all. Both refusals are correct; the writer had nothing citable to anchor its numbers to because the pack held table-of-contents fragments (A49's splitter failure, fixed). Watch-and-re-measure after the splitter fix rather than a code change now — but two schema-level defects rode along and are separately fixable: Capital Allocation died on claims naming *zero* figure ids ("names exactly one figure — not 0"), and Earnings Quality was refused for `figures[0].value` containing the numeral "0" — the digit zero, treated as an unsupported quantity. **The zero half is delivered with A48's span work** (a standalone "0" no longer trips the scan; a zero rate still does). **The claim-shape half needs no fix:** a numeric claim naming zero figure ids is refused by `ProposedClaim._stands_on_the_right_thing` in the response schema, and the field-level refusal already reaches the retry — the rule is right and the ladder works, so what defeated Capital Allocation was the pack, not the rule. **The starved-pack half still needs a live run — but the run could not have answered it.** Reading the code for how that outing would be read back found the measurement missing. The record said whether a pack was *truncated*, which is a different question: a section dealt three excerpts from a pool of three is not truncated and is still starved. And a **failed** section — the case where supply most needs explaining — recorded neither the truncation flag nor any count, so "were these five starved?" was answerable only from a log line written when a section *succeeds*. `EvidenceDealt` now counts what each section was handed by kind — facts, calculations, excerpts; three numbers rather than a total, because a section with facts and no excerpts fails differently from one with excerpts and no calculations — and both drafting boundaries record it on both outcomes, in the log and in the step output. `None` is kept distinct from zero, so a section refused before a pack was built reports no measurement rather than an empty one. The re-measure itself is the operator's next run. |
| A64 | **The platform asks a bank for accounts it does not keep** | `thin_for_forecast: ["current_assets", "current_liabilities", "operating_income"]` — a bank has no classified balance sheet, so current assets and liabilities are not thin, they are *undefined*; the forecast machinery is asking for concepts the filer is forbidden by its own accounting to report. The DCF method note tells the same story backwards: "the income statement shows no interest expense" — for a bank that paid $1,585m of interest in the half, visible in the same run's cash flow facts; interest expense for a bank is an operating line under a different tag, not absent. And the recorded `debt_to_equity` of 0.20–0.50 excludes deposits, which the red team correctly called "almost none of a bank's leverage" against $191.3bn of liabilities. `segments.swept unmapped_tags=1460` and `unplaced=431` (against AAPL's 175) size the same vocabulary gap. Fix: sector-aware expectations — Task 28's sector machinery already classifies the company, so the forecast driver set, the cost-of-debt derivation and the leverage ratio choice should consult it: a depository's drivers are earning assets, deposit cost and provision, not capex intensity; its leverage is assets over equity or the regulatory ratios, never debt-to-equity. **Two of the three are fixed; the third is not where this row put it.** A `SectorProfile` can now state what its accounting does not define. `undefined_concepts` is **not** a list of lines a filer omitted: a bank keeps an unclassified balance sheet and has no operating-income line, so those three concepts do not exist for it, and measuring their coverage put them at zero and reported them as thin — which reads as "this filer discloses poorly" about a bank keeping its accounts exactly as it must. Coverage now measures only what the sector defines, and the run records the lines it never asked for beside the ones it did, so "why only five drivers?" is answerable from the row. `not_meaningful_ratios` carries a ratio key and the reason it misleads here; such a ratio is **not computed at all**, so no calculation row exists for a citation to reach, and it comes back absent carrying the reason rather than a false "this filing does not report total debt". Banks and insurers are populated; the other six profiles are left empty deliberately, because asserting what a REIT or a miner does not define would be this same finding in reverse. The judgement stays in `core/sectors.py` and the arithmetic in `calc/ratios.py`, which takes the exclusion as data — that module knows division, not industries. **The cost-of-debt symptom is downstream of classification, not of the derivation.** For a confirmed bank the DCF is refused twice over before `_cost_of_debt` is ever reached — by `dcf_permitted` and again by the mandate — each with a sector reason. A run whose method note blamed an absent interest expense was therefore a run that was not classified as a bank, so the fix belongs to classification or to A55's vocabulary, and patching the refusal's wording would have hidden that. Not settled here: the run's data is gone with the container, so which of the two it was cannot be read back. **Still its own task, as this row said:** a depository's drivers are earning assets, deposit cost and provision, and nothing above proposes one — what is fixed is the platform stating falsehoods about a bank, not the platform modelling one. |
| ~~A65~~ | ~~**The ADR 0057 cut strands sentence fragments in published prose**~~ | Business Overview ends "…following its prior combination with People's United. **bank by size.** A fuller account…" — the numeral-bearing span was excised mid-sentence and the tail left standing. The salvage that ADR 0057 chose (cut, do not discard) is right; the cut is wrong: it removes the flagged span, not the sentence containing it. Fix: widen every excision to sentence boundaries — drop the whole sentence a flagged span sits in, so what survives is prose a person could have written. Same rule for the length cut, which already drops trailing sentences and is fine; this aligns the numeral cut with it. **Delivered — and the defect was one level down from the finding.** The cut was already sentence-shaped; the sentence *splitter* treated an abbreviation's full stop as a boundary, so "a top-20 U.S. bank by size." split at "U.S." and the removal took half the sentence — the one direction the cut must never err. The splitter now refuses to break after an initialism (matched by shape) or the abbreviations company prose actually uses (Inc., Corp., Ltd., No., e.g., i.e. and kin), each a fixed-width lookbehind. The same splitter serves the gap-sentence count, which inherits the fix. Mutation-verified both ways. |
| ~~A66~~ | ~~**Two adjacent tables, two scales, no units, and one empty cell rendered as its own footnote**~~ | Balance Sheet's Financial History prints `Total assets 219.3` (billions); Cash Flow's prints `Net cash from operating activities 2,280` (millions); neither table carries a unit anywhere. `Accumulated other comprehensive income (loss) -92` sits beside `Total equity 27.9` in the same table — the AOCI row is in *hundreds of millions* of the equity row's scale reading. And `Retained earnings 22 22` renders a missing value as the footnote marker twice. Elsewhere Management & Governance writes `$442,000,000` where the front page writes `$442m`. Fix: the Financial History table states its unit in the header (the values already carry units internally — this is a rendering choice); a row whose value did not resolve is omitted rather than rendered empty; and the house style's money formatter is applied everywhere prose interpolates a recorded figure. **Delivered, with one precision.** An absent value now renders as an em dash — never "None", never a blank whose footnote marker stands alone as "22 22" — and carries no footnote, because a citation on an absent figure points at nothing. A stated unit the formatter cannot restate is printed beside the value when it names a currency or a scale ("219.3 USD billions"), and only then: the platform's own type words ("pure", "x") stay out of print, which the golden documents hold byte for byte. And longhand symbol money in prose ("$442,000,000") reads in the house style ("$442m"), with five-figure amounts and their cents left exactly as the writer stated them. Mutation-verified seven ways. |
| A67 | **The request's typed company name leaks into the report** | Prior Research Comparison: "This is the first research run for M&T **Banking** Corporation (MTB)" — the operator's typo, preserved verbatim, in a report whose every other surface uses the SEC-resolved "M&T BANK CORP". The resolved entity name exists on the company row from the moment `sec.entity_resolved` fires. Fix: rendered surfaces read the company's resolved name, never `request.company_name`, which is an input field not an identity. |
| A68 | **The disagreement rendering counts nothing and says so cryptically** | Every disagreement on the approval page and in the appendix carries "0 thesis (T1_REGULATORY)" under both positions — an evidence count of zero rendered as a label fragment, meaning "this position cites no facts", which for a thesis-level disagreement is definitionally true of both sides. Rendering a count that is always zero as if it informed the reader is noise where the page is doing its most important work. Fix: for thesis-level disagreements, drop the count line entirely; for fact-backed ones, render "3 supporting facts, best tier T1". |

**What the run proved, for the record.** The splitter defect (A49) was found by the
instrumentation built for it one session earlier — `filings.excerpted` said 703,420
characters → 9 excerpts, mean 36,003, ceiling never bound — which is the
instrumentation-first posture paying out exactly as designed. The A50/A51/A52/A53/A54
fixes from the AAPL batch all behaved correctly in the wild: the budget was stated and
enforced, the truncation retry halved and salvaged, the DCF filled from the record with
no model call and an honest method note, the zero-peer comps paragraph told the truth,
and every calculation footnote carries its period. The red team remains the strongest
component in the system.

### Data sources

| # | Gap | Notes |
|---|---|---|
| ~~A19~~ | ~~**Bank of England macro adapter not built**~~ | **Closed differently from how it was framed, and the framing was the error.** The BoE adapter is not "not built" — it is *determined against*: ADR 0026 found the Bank documents a CSV download route for programmatic use and disallows that same route in its own `robots.txt`, and reaching it through the unlisted viewer path would be circumventing a stated restriction. So the euro is the pivot instead. `aer.sources.macro.ecb` reads the ECB's daily reference rates through a documented API with no such conflict, and because every ECB rate has the euro on one side, a GBP/USD rate is a **cross** — `aer.calc.fx.cross` divides two published figures as a *traced calculation*, so a derived rate never looks published. ADR 0045. **The GBP risk-free rate is still missing** and this did not fix it: the UK proxy is a gilt yield, which is BoE data, so `risk_free_series_for("GBP")` still refuses rather than discounting sterling at a US Treasury yield. |
| ~~A20~~ | ~~**EODHD unresolved**~~ | **Resolved: ADR 0030 route 2** — keep the personal subscription, build for internal use only, accept explicitly that a future commercial version needs a different licence. Most of what the route required was already built by the tasks that followed the ADR: the comps table returns a `WithheldComps` with *no rows* to a shareable audience (0034), exportable and internal charts are separate functions with a render-time refusal, the corrected licence note is pinned, and the weighted daily-call ledger reserves before the request. **One thing was missing and it was the load-bearing one:** `purge_provider` had no caller anywhere in `src/`, so the deletion the agreement requires within a month of the subscription ending could be performed only by writing Python at a REPL. `aer purge-licensed` is that caller, and a test over the policy table fails if a second paid feed is ever added without one. The derived-data question was then **settled by operator determination on 2026-08-09** (ADR 0030, amended): figures computed from the data may be published, on the operator's reading of the executed agreement rather than on an inference from the public terms — and the licence note says which. The permission does not extend to the series itself or a chart of it, so the price chart stays internal. The withholding machinery survives the decision that opened it, defaulting closed, so the next paid feed inherits nothing. |
| ~~A47~~ | ~~**The EODHD adapter calls an endpoint the subscription does not include**~~ | **Closed, and the mechanism was not the one recorded here.** The claim was that `/api/fundamentals` is "the only source of the share count"; it never was. `acquire_prices` has always taken a `shares_outstanding` argument and preferred it — "the filed figure is a fact with a hashed source behind it, and the vendor's is a convenience" — and **nothing ever passed one**, so every run fell through to the vendor. A wiring gap of exactly the A1 shape, in a service written to avoid it. The prices step now reads the count from the run's own facts: newest observation wins rather than newest fiscal year, because a market capitalisation wants the count as it stands and `dei:EntityCommonStockSharesOutstanding` — dated the day the annual report was signed — is both the freshest and a fact with an archived filing behind it. The point-in-time bound applies to it as to any fact, and it is deliberately not restricted to annual periods: after A45 an instant defines no period, and that rule is about which years have statements rather than which share count is current. The vendor path stays for an account that carries the feed, and the entitlement table in `docs/data-sources/eodhd.md` records that this one does not. Pinned by a test that the vendor is not asked when the filings answer. |

---

## List B — features, from the operator's seat

### Broken or absent in what already exists

| # | Gap | Notes |
|---|---|---|
| ~~B1~~ | ~~**No way to delete or archive a research request**~~ | **Closed, with both verbs.** Archive is one click on the list, destroys nothing and is undone by one more; it is accepted whatever state the request is in, which is the case `delete_request` had to refuse. Remove is a confirmation page listing what would be destroyed by table, what survives, and what the run cost — then deletes the request and everything derived from it, walking the schema's own dependency sort. The audit chain, the spend ledger and the content-addressed artefacts all survive, and a purge is refused when a later run's claims cite evidence this one gathered, which is the common case for a company researched twice. |
| ~~B2~~ | ~~**Valuation, DCF, scenarios and sensitivities never appear in a run**~~ | **Closed.** Six assumptions are derived from the filings with their derivation stated, two are proposed by the ADR 0046 role under bounds that refuse rather than clamp, and the three the discount rate decomposes into are named as outstanding with a reason — or proposed, in beta's case, once prices are acquired. A conditional `ASSUMPTIONS` gate stops the run only when the set is complete, because a gate an operator cannot clear is a run that pauses and never resumes; the create route is what lets them clear it. The `value` step then runs the WACC, the DCF, the scenarios and two sensitivity grids, every figure a recorded calculation. |
| ~~B3~~ | ~~**Comparables never appear**~~ | **Closed at the reachable scope.** `acquire_prices` fetches the subject's series and its market's, stores both as hashed artefacts under the licensed tier, computes a market capitalisation and proposes a beta — which is what makes B2's gate reachable without an operator typing one. The market proxy is a documented decision per exchange and an undocumented exchange is refused, because a London share regressed against the S&P looks exactly like one regressed against the All-Share. `comps` then builds the table. **It is thin, and honestly so:** a peer's multiple needs that peer's filings and prices, this workflow acquires neither, so every confirmed peer is excluded *by name with the reason*. Acquiring peer data is a larger job than B3 and is Task 30's. |
| ~~B4~~ | ~~**A run reads one filing**~~ | **Closed.** A run now reads the annual report and the recent current reports as well as the aggregate. Whether the recent-developments worker does better with them is the next live run's question. |
| ~~B13~~ | ~~**No full-text search**~~ (IR discovery deliberately deferred) | **Half closed, on purpose.** `search_filings_full_text` is now a worker tool: the model supplies the phrase, code supplies the CIK and the as-of bound, and hits come back as metadata — form, date, URL — so reading one still costs a `fetch_known_url` call and the twelve-call budget keeps meaning something. Hits after the as-of date are counted and named rather than dropped, because "found nothing" and "found things you may not read" call for different next moves. **Issuer-IR discovery stays uncalled by choice**: `aer.sources.issuer` holds that no code path learns a domain from a page and then fetches it, and a discovery step is exactly that path with a search engine in the middle. |
| ~~B5~~ | ~~**Charts have nothing to plot**~~ | **Closed by A1**, at least in principle: a run now records a multi-period ledger, so the revenue-and-margin history has a series behind it. The scenario and sensitivity charts stay placeholders until the valuation does. |
| ~~B14~~ | ~~**The assumptions form takes a free-text unit and an unbounded number**~~ | **Closed, with one claim here corrected.** The note said `%` was "accepted at entry and raises mid-valuation" — it was not: `_require_unit` has always parsed the unit at entry and refused what the algebra cannot read. The two real holes were narrower and the second is the dangerous one. **A unit could be readable and still wrong**: `USD` against a tax rate parses perfectly, survived entry and failed much later inside arithmetic that cannot say which input was the mistake. **And the scale had no check at all**: `4.5` where `0.045` was meant is well formed, in the right unit, and discounts at 450%. `aer.core.assumption_scales` now holds the expected unit and a plausible band per name — generous, so a loss-maker's margin and a doubling growth rate pass and only a factor of a hundred does not — and `assumptions.propose` refuses on either, so the JSON API and the form inherit one rule. Refused rather than clamped, on ADR 0046's reasoning, with `accepted_anyway` for the operator who means it, because a check nobody can get past is the gate-nobody-can-clear failure in a smaller costume. The form offers the vocabulary as a list defaulted to `pure`, states the decimal-fraction convention, and carries the override. The table lives in `core` because `assumption_gate` imports `assumptions`, so it had to sit below both; a test pins its keys to `REQUIRED_NAMES`. |

### Not built (Phase 6)

| # | Gap | Notes |
|---|---|---|
| ~~B6~~ | ~~**Settings screen**~~ | **Closed for cost and method; credentials deliberately excluded — ADR 0050.** `/settings` edits model routing, the per-run and monthly budgets and the warning ratio, stored in `settings_overrides` and applied to runs that *start* after the change (a run whose routing moved mid-flight would have a record describing two platforms). Credentials stay in `.env` and are shown as present-or-absent only: A10's `aer backup` pg_dumps the database, so a key in a settings table is a key in every backup. The allowlist is enforced at the write, and a test walks `Settings` for every `SecretStr` field to assert none is overridable — so a credential added next year cannot quietly become editable. |
| ~~B7~~ | ~~**Cost dashboard**~~ | **Closed with A15.** `/costs` shows total spend, spend by category, a per-role breakdown split by model, and the prompt-cache hit rate. |
| ~~B8~~ | ~~**"Reproduce this run" button**~~ | **Closed.** A POST from the run console to `/runs/{id}/replay`, rendering A12's four legs. A POST rather than a link because re-verifying a citation writes its verdict back — it reads like a report but changes stored state, and on loopback with no auth a plain link would let any open tab rewrite verification state. Offered whatever the run's status: a failed or cancelled run is often the one worth interrogating. |
| B9 | **Watchlists and scheduled runs** | APScheduler in the worker. |
| ~~B10~~ | ~~**Skill export/import with a confirmation diff**~~ | **Closed.** Import with a diff and a hash-checked confirmation already existed (T20); what was missing was export and anything to start from. `GET /skills/{key}/export` returns the stored *source* byte for byte rather than a re-serialisation of the parsed frontmatter — a re-serialisation would reorder keys and drop comments, so a round trip would show a diff full of changes nobody made, and the diff is the whole control. Three worked examples ship under `aer.skills.examples`, listed at `/skills/examples`; **none is installed**, and choosing one goes through the same import-and-confirm path a file from a stranger takes, because pre-installing them would make that step look optional for exactly the files an operator trusts most readily. |
| ~~B11~~ | ~~**Provider and model configuration UI**~~ | **Closed with B6** for model and effort per role. Provider *keys* are out of scope by the same ADR 0050 reasoning; if wanted, the honest form is an OS keyring rather than a database column. |

### Deliberately out of scope

| # | Item | Notes |
|---|---|---|
| B12 | **Multi-company and portfolio views** | Named here so it stays a decision rather than an oversight. `docs/PLAN.md` keeps the platform to one report at a time. |

---

## Corrections to the task record

`docs/PLAN.md` Stage 4's task list and the working task board disagreed with
`docs/phase-3-plan.md`, which records outcomes against each task. The phase plan was right, and
**the board has since been reconciled to it (2026-08-20)** — tasks 26 to 32 marked delivered
against the phase plan's own outcome notes, and task 25 rewritten down to the one thing still
genuinely open. `docs/PLAN.md`'s Phase 3 deliverables now name the ADRs that superseded them,
because CLAUDE.md makes the plan the authority on scope and a reader following it alone would
otherwise go and build an adapter this project has refused:

* **Task 26 (cost of capital)** — done. `src/aer/calc/wacc.py`.
* **Task 27 (the discounted cash flow)** — done. `src/aer/calc/dcf.py`, `services/valuation.py`.
* **Task 28 (sector enforcement)** — done.
* **Task 25 (macro with vintages)** — ALFRED and ONS done. The Bank of England is not
  outstanding but *refused* (ADR 0026); the euro reference rates replace it as the FX
  source (ADR 0045), and the GBP risk-free rate remains genuinely open — `RISK_FREE_SERIES`
  has no GBP key, so a sterling valuation refuses rather than discounting at a US Treasury
  yield. Closing it needs a person, not code: the Bank's own conflict resolved in writing,
  the UK DMO's terms read for the first time, or a euro-crossed rate accepted and recorded
  as a calculation rather than an observation.

* **Tasks 29–32** — this section said they "remain genuinely outstanding", and the phase
  plan it names as the authority records all four **delivered** (29–31 on 2026-08-05, 32 on
  2026-08-06), verified again on 2026-08-18 by their own suites: prices and actions with the
  PIT clamp in the adapter, the comps table with each confirmed peer priced and read from
  its own filings (gap S3), the valuation surface reading from the ledger, and the replay
  harness with the gate at ten blocking metrics against the acceptance's eight. The one
  scoped remainder was recorded on `propose_peers_from_sic` itself: peers were proposed only
  from companies this database already holds, so a fresh database proposed nobody — honest,
  and deliberate, but it meant no run had ever produced a comps table. **Closed on
  2026-08-18 by ADR 0059**: the `peer_proposal` role names comparables by ticker with a
  rationale, `aer.services.peer_discovery` resolves each one against EDGAR and refuses what
  it cannot find, and a resolved peer's facts are acquired down the subject's own chain. The
  SIC lookup stays underneath as the fallback, and the gate is unchanged — the confirmation
  and the refusal were always indifferent to who proposed.

---

## Suggested order

**A1 to A4, A21, B13, A9, B1, A19, A20, B2 and B3 are done, and the whole test-suite health
section — A16, A17 and A18 — is now closed.** That last one is worth a sentence of its own:
until A16 was fixed the suite could not complete in one process, so every "verified" claim
in this document rested on a subset. It completes now, which is what makes the rest of these
entries checkable.

What that leaves, in the order it is worth doing (A14 came off this list late: it was
closed by ADR 0048 and the entry above says how, but this section still recommended it —
the order is a summary, and a summary that contradicts its own table is worse than none):

1. **A5, A7 and A8 — authentication, inbound rate limiting and a deployment story.** One
   decision rather than three tasks: they matter exactly when this stops being a tool on
   one machine, and not before. `docs/PLAN.md` keeps them behind a flag for that reason.
2. **B9 — watchlists and scheduled runs**, the last unbuilt item of Phase 6's list.
3. ~~Peer discovery~~ — **done on 2026-08-18** (ADR 0059). It stood here as a decision
   rather than a task, and the decision was taken: a model proposes, the registry resolves,
   a person still confirms. One consequence worth knowing before the next live run — the
   peer-set gate now fires on an ordinary run, where it used to pass straight through on an
   empty proposal, so a run stops once more than it did.
4. The rest of Phase 6 in the order `docs/PLAN.md` gives. **A10 to A12 are done** —
   backups with a verifier and a working restore, audit-chain verification, and run-level
   replay — which closes the whole data-lifecycle section. What A9 made pressing is now
   answered: the sweep can tell an operator the store has lost a document, and there is
   something to restore it from.
