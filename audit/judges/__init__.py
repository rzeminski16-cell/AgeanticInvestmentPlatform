"""The judging panel, as code rather than as its own output.

September's panel exists only as ``audit/out/judges/reads.json``. A repeat of it would be a
different instrument wearing the same name, so anything that re-uses those judgements reads
that file rather than re-deriving it.
"""
